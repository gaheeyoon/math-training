# 운영·배포 가이드 (운영자 전용)

가히가 사이트를 운영하면서 보는 문서입니다.
사이트 배포, 주차별 코드 발급, 보호자 응대, 학기 추가 방법을 담았습니다.

**보호자와 아이가 보는 사용법은 [README.md](README.md) 를 보세요.**

### 누가 무엇을 하나

| 역할 | 하는 일 | 보는 곳 |
|---|---|---|
| **운영자 (가히)** | 주차별 코드 확인, 블로그 댓글에 코드 안내, 학기 추가 | `admin.html` · 이 문서 |
| **보호자** | 블로그 댓글로 코드 문의 → 받은 코드로 입장 | 학습 페이지 · README |
| **아이** | 문제 풀이 | 학습 페이지 |

운영자 페이지 주소와 비밀번호는 **누구에게도 알려주지 않습니다.**
보호자에게 전하는 것은 여덟 글자 코드뿐입니다.

---

## 1. 깃허브에 배포하기

이미 배포되어 있다면 **2번(수정하고 다시 올리기)** 으로 건너뛰세요.

### 방법 A — 스크립트로 한 번에 (권장)

[GitHub CLI](https://cli.github.com) 를 설치하고 한 번만 로그인해 둡니다.

```bash
gh auth login
```

그다음 저장소 폴더에서 아래 두 줄이면 끝입니다.

```bash
python3 tools/build_auth.py --admin-pass '원하는_관리자_비밀번호'
bash tools/deploy.sh math-training public
```

스크립트가 저장소 생성 → 푸시 → GitHub Pages 설정(main / `/docs`)까지 하고
마지막에 주소를 알려줍니다. 평문 데이터가 섞여 들어가지 않는지도 한 번 더 확인합니다.

```
학습 페이지  : https://yoongahee.com/math-training/
관리자 페이지: https://yoongahee.com/math-training/admin.html
```

---

### 방법 B — 손으로 하기

#### ① 저장소를 만들고 파일을 올립니다

```bash
git init
git add .
git commit -m "계산 트레이닝 사이트"
git branch -M main
git remote add origin https://github.com/<아이디>/<저장소이름>.git
git push -u origin main
```

`.gitignore`가 `source/` 폴더를 자동으로 제외하므로,
**평문 문제 데이터는 깃허브에 올라가지 않습니다.**

#### ② GitHub Pages를 켭니다

저장소 → **Settings → Pages** 에서

- **Source**: `Deploy from a branch`
- **Branch**: `main` / 폴더는 **`/docs`** 선택

저장하고 1~2분 기다리면 아래 주소로 열립니다.

```
https://yoongahee.com/math-training/
관리자 페이지: https://yoongahee.com/math-training/admin.html
```

> 반드시 `/docs` 폴더로 지정하세요. 루트로 지정하면 `tools/`까지 웹에 노출됩니다.

> 저장소를 공개(public)로 둬도 괜찮습니다. `docs/` 안에는 암호화된 파일만 있고,
> 평문 데이터가 든 `source/`는 올라가지 않기 때문입니다.

---

## 2. 수정하고 다시 올리기

이 폴더가 곧 깃허브 저장소입니다. 파일을 고친 뒤 아래 세 줄이면 사이트에 반영됩니다.

```bash
git add -A
git commit -m "무엇을 바꿨는지 한 줄"
git push
```

푸시하고 1분쯤 지나면 사이트가 새 내용으로 바뀝니다.

**지금 무엇이 바뀌었는지 보고 싶을 때**

```bash
git status          # 바뀐 파일 목록
git diff            # 바뀐 내용
git log --oneline   # 지금까지의 기록
```

**되돌리고 싶을 때**

```bash
git restore <파일>          # 아직 커밋 안 한 변경 되돌리기
git revert HEAD             # 마지막 커밋을 취소하는 새 커밋 만들기
```

> `source/` 폴더는 `.gitignore` 로 제외되어 있어 `git add -A` 를 해도 올라가지 않습니다.
> 평문 문제 데이터가 들어 있으니 그대로 두세요.

---

## 3. 인증 코드 발급하기

### 처음 한 번

```bash
pip install cryptography
python3 tools/build_auth.py --admin-pass '원하는_관리자_비밀번호'
```

실행하면 52주치 코드를 만들고, 화면에 이번 주와 다음 주 코드를 보여줍니다.
그다음 `docs/` 안의 바뀐 파일을 커밋해서 올리면 끝입니다.

```bash
git add docs/data && git commit -m "인증 코드 갱신" && git push
```

### 자주 쓰는 옵션

| 옵션 | 뜻 | 예시 |
|---|---|---|
| `--admin-pass` | 운영자 페이지 비밀번호 (필수) | `--admin-pass 'my-secret-1234'` |
| `--weeks` | 몇 주치를 미리 만들지 (기본 52주) | `--weeks 104` |
| `--start` | 1주차 시작일 (월요일 권장) | `--start 2026-08-10` |
| `--grace` | 지난 주 코드도 받아줄지 | `--grace 0` (이번 주만 허용) |
| `--data-only` | 코드는 그대로, 데이터만 재암호화 | `--data-only` |

> **주의** — 옵션 없이 다시 실행하면 **모든 주차의 코드가 새로 만들어집니다.**
> 학기 중에는 되도록 실행하지 마시고, 남은 주차가 얼마 없을 때 `--weeks`를 늘려 재발급하세요.

### 코드는 그대로 두고 문제만 바꾸고 싶을 때

문제를 고치거나 학기를 추가할 때는 `--data-only` 를 붙이세요.
이미 나눠 준 인증 코드가 그대로 유지된 채 학습 데이터만 다시 암호화됩니다.

```bash
python3 tools/build_auth.py --admin-pass '관리자_비밀번호' --data-only
```

### 운영자 비밀번호를 바꾸려면

```bash
python3 tools/build_auth.py --admin-pass '새_비밀번호'
```

비밀번호는 어디에도 저장되지 않습니다. 잊어버리면 코드를 다시 발급하는 수밖에 없습니다.

---

## 4. 주마다 하는 일 — 코드 안내

보호자가 **블로그 댓글로 코드를 문의**하면, 운영자 페이지에서 그 주 코드를 확인해 답글로 알려줍니다.

### 코드 확인하기

```
https://yoongahee.com/math-training/admin.html
```

1. 위 주소를 열고 운영자 비밀번호를 입력합니다
2. 이번 주 코드가 큰 글씨로 나옵니다
3. **댓글 답변 문구 복사** 를 누르면 아래 같은 문장이 통째로 복사됩니다

```
안녕하세요! 이번 주(8월 10일 ~ 8월 16일) 인증 코드는 9DWS-7645 입니다.
아래 주소에서 코드를 입력하시면 바로 들어가실 수 있어요.
https://yoongahee.com/math-training/
코드는 매주 월요일에 바뀌니, 다음 주에 다시 댓글 남겨주시면 알려드릴게요.
```

댓글 답글에 그대로 붙여 넣으면 됩니다.
코드만 필요하면 **코드만 복사** 를 쓰세요. 아래 표에서 다음 주 코드와 전체 일정도 미리 볼 수 있습니다.

### 알아두면 좋은 것

- 한 번 인증한 브라우저는 **그 주 일요일 밤까지** 코드를 다시 묻지 않습니다.
  보호자는 일주일에 한 번만 문의하면 됩니다.
- 지난주 코드도 한 주 동안은 통과합니다(`--grace 1`, 기본값).
  월요일에 바로 답을 못 드려도 학습이 끊기지 않습니다.
- 두 주 이상 지난 코드는 막힙니다. 화면에 "이미 지난 N주차 코드입니다"라고 안내됩니다.
- 코드를 물어보는 곳을 화면에도 적어 둘 수 있습니다.
  `docs/data/manifest.js` 의 `contact` 에 블로그 주소를 넣으면 잠금 화면에 문의 버튼이 생깁니다.

```js
contact: {
  note:  '코드는 매주 월요일에 바뀝니다.',
  guide: '코드를 모르시면 블로그 댓글로 문의해 주세요.',
  label: '블로그에서 코드 문의하기',
  url:   'https://blog.naver.com/아이디/223456789'   // ← 여기에 글 주소
}
```

---

## 5. 인증이 어떻게 동작하나요

정적 사이트에는 서버가 없어서, 흔히 쓰는 "비밀번호가 맞으면 화면을 보여주는" 방식은
소스만 열어보면 뚫립니다. 그래서 **화면을 가리는 대신 데이터 자체를 잠갔습니다.**

```
문제 데이터  ──[ AES-256-GCM ]──►  docs/data/3-1.enc.js
                    ▲
              콘텐츠 키(CK)
                    ▲
        ┌───────────┴───────────┐
   1주차 코드로 감싼 CK    2주차 코드로 감싼 CK   …  (docs/data/keys.js)
        ▲                       ▲
   PBKDF2-SHA256 (31만 회 반복)
```

- 코드를 입력하면 브라우저가 PBKDF2로 키를 만들어 그 주의 자물쇠를 열어보고,
  열리면 콘텐츠 키를 얻어 문제를 복호화합니다.
- **코드가 없으면 복호화가 불가능합니다.** 개발자 도구로 파일을 열어봐도 암호문뿐입니다.
- 이번 주(설정에 따라 지난 주까지) 코드만 받아들이므로, 오래된 코드는 자동으로 막힙니다.
- 코드는 헷갈리는 글자(`I` `O` `0` `1`)를 뺀 32글자에서 8자리를 뽑습니다
  (`XXXX-XXXX`, 약 1조 가지). 대소문자와 하이픈은 알아서 맞춰 줍니다.

### 솔직하게 알아두실 점

- 한 번 인증한 사람이 **브라우저에 저장된 콘텐츠 키를 직접 꺼내면**, 그 뒤로는 코드 없이도
  데이터를 풀 수 있습니다. 마음먹은 기술자를 막지는 못합니다.
  이 잠금장치는 *링크가 퍼져서 아무나 들어오는 상황*을 막기 위한 것입니다.
- 더 강하게 막아야 한다면 주차마다 데이터를 따로 암호화하거나(용량이 주차 수만큼 늘어납니다),
  로그인 서버를 두는 방식으로 가야 합니다.
- `admin.html` 주소는 검색에 노출되지 않도록 `noindex`를 걸어두었지만,
  주소를 아는 사람은 페이지를 열 수는 있습니다. 실제 보호는 **비밀번호**가 담당하므로
  주소와 비밀번호를 함께 알려주지 마세요.
- 암호 기능은 보안 연결에서만 동작합니다. GitHub Pages는 https라 문제없지만,
  로컬에서 `index.html`을 더블클릭해 열면 동작하지 않습니다 (아래 5번 참고).

---

## 6. 로컬에서 확인하기

```bash
cd docs
python3 -m http.server 8000
```

브라우저에서 `http://localhost:8000` 으로 접속하세요.
`localhost`는 보안 연결로 취급되어 암호 기능이 정상 동작합니다.

---

## 7. 폴더 구조

```
├─ .gitignore              source/ 를 깃허브에서 제외
├─ README.md               사용법 — 보호자·아이용
├─ MAINTENANCE.md          운영·배포 가이드 — 이 문서
│
├─ source/                 ⚠ 평문 문제 데이터 — 올리지 않음. 따로 백업해 두세요
│   ├─ 1-1.js
│   └─ 3-1.js
│
├─ tools/
│   ├─ build_auth.py       코드 발급 + 데이터 암호화
│   └─ deploy.sh           저장소 생성 + 푸시 + Pages 설정 (한 번에)
│
└─ docs/                   ← GitHub Pages 가 서비스하는 폴더
   ├─ index.html           학습 페이지 (인증 화면 포함)
   ├─ admin.html           관리자 페이지
   ├─ assets/
   │   ├─ app.css          사이트 스타일
   │   ├─ app.js           화면 전환 · 문제 출력 · 채점
   │   ├─ auth.js          주차별 인증 · 복호화
   │   ├─ admin.js         관리자 화면
   │   ├─ print.js         A4 인쇄 조판
   │   ├─ print.css        인쇄 전용 스타일
   │   └─ concept.css      '원리 한 장' 디자인 (인쇄본과 동일)
   └─ data/
       ├─ manifest.js      학기 목록      ← 학기를 추가할 때 여기를 고칩니다
       ├─ keys.js          주차별 자물쇠  (자동 생성)
       ├─ adminvault.js    운영자 금고    (자동 생성)
       ├─ 1-1.enc.js       암호화된 문제  (자동 생성)
       └─ 3-1.enc.js       암호화된 문제  (자동 생성)
```

---

## 8. 다른 학기 추가하기

### ① `source/` 에 문제 데이터 파일을 넣습니다

파일 이름은 `<학기ID>.js` 로 맞춥니다. 예: `source/3-2.js`

### ② `docs/data/manifest.js` 에 한 줄 추가합니다

```js
{
  id: '3-2',                    // source/3-2.js 의 이름과 같아야 합니다
  grade: 3, term: 2,
  title: '3학년 2학기',
  subtitle: '큰 수의 곱셈과 나눗셈',
  desc: '한 줄 설명',
  status: 'ready'               // 'ready' = 공개 / 'soon' = 준비 중(회색 카드)
}
```

지금은 1학년 1학기와 3학년 1학기가 열려 있고, 3학년 2학기와 4학년 1·2학기가
`'soon'` 상태로 미리 들어가 있습니다.

### ③ 다시 빌드하고 올립니다

```bash
python3 tools/build_auth.py --admin-pass '관리자_비밀번호' --data-only
git add docs && git commit -m "3학년 2학기 추가" && git push
```

### ④ 데이터 파일 형식

```js
window.registerSet({
  id: "3-2",
  title: "3학년 2학기",
  subtitle: "큰 수의 곱셈과 나눗셈",
  desc: "설명",
  steps: [
    {
      no: 1,
      title: "회차 제목",
      tag: "짧은 분류",
      goal: "학습 목표 한 문장",
      concept: {
        title: "원리 페이지 제목",
        sub:   "부제",
        html:  "<div class='csec'>…</div>",   // concept.css 클래스를 그대로 씁니다
        tip:   "보호자용 안내 문구"
      },
      days: [
        {
          day: 1,
          sheets: [
            { key: "A", label: "시트 제목", type: "mulh", items: [[12,4], [23,3]] },
            { key: "B", label: "시트 제목", type: "mulv", digits: 2, items: [[20,3]] }
          ]
        }
      ]
    }
  ]
});
```

### ⑤ 쓸 수 있는 문제 유형(`type`)

| type | 설명 | `items` 한 개의 형태 |
|---|---|---|
| `mulh` | 곱셈 가로셈 | `[24, 3]` |
| `mulv` | 곱셈 세로셈 (`digits` 필요) | `[24, 3]` |
| `divh` | 나눗셈 가로셈 | `[42, 6]` |
| `divv` | 나눗셈 세로셈(장제법) | `[42, 6]` |
| `family` | 곱셈식 → 나눗셈식 두 개 | `{d:3, q:4, p:12}` |
| `bundle` | 묶음 표 채우기 | `{total:12, per:3, cnt:4, hidden:1}` |
| `unit` | 시간·길이의 합과 차 | `{op:'+', u1:'분', u2:'초', base:60, a:[18,45], b:[7,30]}` |
| `eq` | □가 있는 식 (곱셈·나눗셈) | `{t:'mr', x:6, y:42}` |
| `part` | 가르기·모으기 막대 | `{w:5, a:2, b:3, hidden:0}` |
| `arith` | 한 자리 덧셈·뺄셈 가로셈 | `[3, '+', 4]` |
| `atable` | 덧셈표 · 뺄셈표 | `{op:'+', rows:[1,2,3,4,5], cols:[1,2,3,4]}` |
| `eq1` | □가 있는 식 (덧셈·뺄셈) | `{t:'ar', x:3, y:8}` |
| `numfam` | 세 수로 만드는 네 개의 식 | `{a:2, b:5, c:7}` |
| `seq` | 수의 순서 채우기 | `{start:17, hide:[1,3,4]}` |

`bundle`의 `hidden` 은 가릴 칸(0=전체 개수, 1=한 묶음의 개수, 2=묶음 수),
`part`의 `hidden` 은 0=전체, 1=왼쪽, 2=오른쪽,
`eq1`의 `t` 는 `ar`(x+□=y) · `al`(□+x=y) · `sr`(x−□=y) · `sl`(□−x=y),
`t` 는 □의 자리(`mr`: x×□=y, `ml`: □×x=y, `dd`: □÷x=y, `dv`: x÷□=y)를 뜻합니다.

정답은 데이터에 적지 않습니다. 위 정보만으로 화면에서 **계산해서 채점**하므로
문제와 정답이 어긋날 일이 없습니다.

---

---

인쇄 방법과 학습 화면 사용법은 [README.md](README.md) 를 참고하세요.
